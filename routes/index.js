// const express = require('express');

// const productsRouter = require('./products.router'); // Asegúrate de que products.router esté en la ubicación correcta
// const cors = require('cors');
// app.use(cors());

// function routerApi(app) {
//   // app.use('/api', router); // Usa el enrutador API existente

//   // Agrega el enrutador de productos bajo la ruta /products
//   app.use('/products', productsRouter);
// }

// module.exports = routerApi;
